{
  "name": "panicdna-ax-monitor",
  "owner": {
    "name": "panicdna",
    "url": "https://github.com/panicdna"
  },
  "description": "AX usage measurement for Claude Code. A SessionEnd/Stop hook ships each raw session transcript to a measurement server with zero workflow change (upload only — no parsing or LLM on the client), and a bundled stdlib local verifier lets you test the flow without a full backend. Bundles its own install/uninstall skills.",
  "plugins": [
    {
      "name": "ax-monitor",
      "source": "./plugins/ax-monitor",
      "description": "Claude Code SessionEnd/Stop hook that POSTs the session transcript to an AX measurement server. Optional per-turn rows, call logging, and a bundled local verifier. Bundles the 'install-ax-monitor' and 'uninstall-ax-monitor' skills."
    },
    {
      "name": "ax-monitor-server",
      "source": "./plugins/ax-monitor-server",
      "description": "The receiving end: a single-file stdlib retail server that captures the raw transcript the ax-monitor hook sends, prints a deterministic breakdown, optionally summarizes and forwards, and serves a read-only dashboard. Bundles 'install-ax-server' / 'uninstall-ax-server' to install it as a nohup-managed daemon with a configurable bind IP and port (default 14200)."
    }
  ]
}
